export type TUserFilters = {
  globalSearch?: string;
}

export type TPaginationOptions = {
  page?: number;
  pageSize?: number;
};

export type TSortOptions = {
  sortBy?: string;
  sortOrder?: string;
};